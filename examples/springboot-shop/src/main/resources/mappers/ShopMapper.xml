<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="local.xmock.ShopMapper">
  <resultMap id="productRow" type="local.xmock.ShopRepository$Product">
    <constructor>
      <idArg column="id" javaType="_long"/>
      <arg column="name" javaType="string"/>
      <arg column="price_cents" javaType="_long"/>
      <arg column="stock" javaType="_long"/>
      <arg column="status" javaType="string"/>
    </constructor>
  </resultMap>
  <resultMap id="cartRow" type="local.xmock.ShopRepository$CartItem">
    <constructor>
      <idArg column="id" javaType="_long"/>
      <arg column="user_id" javaType="_long"/>
      <arg column="product_id" javaType="_long"/>
      <arg column="quantity" javaType="_long"/>
    </constructor>
  </resultMap>
  <select id="products" resultMap="productRow">
    SELECT id, name, price_cents, stock, status FROM products WHERE status = #{status,jdbcType=VARCHAR} ORDER BY id
  </select>
  <select id="product" resultMap="productRow">
    SELECT id, name, price_cents, stock, status FROM products WHERE id = #{id,jdbcType=BIGINT}
  </select>
  <select id="item" resultMap="cartRow">
    SELECT id, user_id, product_id, quantity FROM cart_items WHERE user_id = #{userId,jdbcType=BIGINT} AND product_id = #{productId,jdbcType=BIGINT}
  </select>
  <select id="cart" resultMap="cartRow">
    SELECT id, user_id, product_id, quantity FROM cart_items WHERE user_id = #{userId,jdbcType=BIGINT} ORDER BY id
  </select>
  <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="local.xmock.CartInsert">
    INSERT INTO cart_items (user_id, product_id, quantity) VALUES (#{userId,jdbcType=BIGINT}, #{productId,jdbcType=BIGINT}, #{quantity,jdbcType=BIGINT})
  </insert>
  <update id="increment">
    UPDATE cart_items SET quantity = quantity + #{quantity,jdbcType=BIGINT} WHERE id = #{id,jdbcType=BIGINT} AND user_id = #{userId,jdbcType=BIGINT}
  </update>
</mapper>
